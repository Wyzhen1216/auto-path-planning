# Phase 4 Scheme A — declarative pipeline + per-stage params

template: single

rationale: "Dijkstra 全局 Phase3 最佳 resolution=0.4"

stages:
  - id: planner
    algorithm: dijkstra
    role: global
    params:
      resolution: 0.4
      robot_radius: 0.45
